[project]
name = "study-pdf-desktop"
version = "0.1.0"
description = "Personal desktop study app for PDF reading and scheduled review"
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.10.0,!=6.10.1",
  "pdfjs-viewer-pyside6>=1.1.2",
  "pypdf>=4.2.0"
]

[project.scripts]
study-app = "study_app.main:run"
